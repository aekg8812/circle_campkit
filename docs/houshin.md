# アウトドアサークル運営アプリ 開発ガイド（メンバー共有用）

このドキュメントは、これから開発を一緒に進めるメンバー全員に向けた案内です。
プロジェクトの前提・環境構築・作業の進め方をまとめています。**まずこれを読んでから作業を始めてください。**

---

## 1. このアプリは何か

アウトドアサークルの運営（計画立案・参加募集・学校提出用計画書の作成・活動記録）を一元化する Web アプリです。
複数のサークルが使える前提で、ユーザーは「グループ（＝サークル）」に参加または作成し、その中に計画（キャンプ・合宿・日帰り）を作っていきます。

- 技術スタック: **Next.js 16 (App Router) + TypeScript / Supabase / Vercel**
- LINE アプリ内での利用に対応しています（**LIFF**・LINE ログイン）
- 一部に **Gemini API** を使った補助機能があります（行程表の下書き・持ち物チェック）
- 本番: https://campkit-king-k-57s-projects.vercel.app
- LINE から開く: https://liff.line.me/2011727048-0Tdp92M9
- 詳細な仕様・データモデル・ロール定義は **`docs/DESIGN.md`** にすべて書いてあります。これが設計の「正」です。迷ったら DESIGN.md を見る、が原則です。

---

## 2. 現在の進捗

| フェーズ | 内容                                               | 状態    |
| -------- | -------------------------------------------------- | ------- |
| Phase 0  | 基盤（環境・認証・DBスキーマ全体・RLS）            | ✅ 完了 |
| Phase 1  | アカウントモジュール A（プロフィール / 車 / 道具） | ✅ 完了 |
| Phase 2  | グループ（作成・参加・一覧・ダッシュボード）       | ✅ 完了 |
| Phase 3  | 計画 + 行程表 B                                    | ✅ 完了 |
| Phase 4  | 募集 C                                             | ✅ 完了 |
| Phase 5  | 計画書 D（PDF・Excel出力）                         | ✅ 完了 |
| Phase 6  | 準備 E + アーカイブ F                              | ✅ 完了 |

当初の Phase 0〜6 はすべて実装済みです。そのうえで、以下を追加しています。

| 追加した機能         | 内容                                                             |
| -------------------- | ---------------------------------------------------------------- |
| LINE 対応            | LIFF でのアプリ内起動、LINE ログイン、招待リンクの LIFF 化       |
| 計画書の様式編集     | 表の項目をグループごとに追加・削除・並べ替えできる               |
| AI 補助              | 行程表の下書き生成 / 持ち物の抜け漏れチェック（Gemini API）      |
| 計画テンプレート     | 実際の行き先をもとにした4種（久住高原・雲仙・野遊び浜・日帰り）  |

### まだ手を付けていないこと

- 計画詳細で、起案者向けの操作が4箇所に散らばっている（まとめたい）
- 計画作成フォームが1画面で長い（折りたたみ・離脱警告を入れたい）
- 募集の定員超過時の競合制御（先着順を厳密にする仕組み）
- アカウント統合（同じ人がメールと LINE で別アカウントになる）

---

## 3. 環境構築（初回だけ）

### 前提ツール

- Node.js（LTS 版）
- Git
- Claude Code
- Supabase のプロジェクトにアクセスできること（下記参照）

### 手順

1. リポジトリを clone する。
   ```
   git clone <リポジトリURL>
   cd <プロジェクト名>
   npm install
   ```
2. `.env.local` を用意する。`.env.local.example` をコピーし、Supabase の接続情報を入れる。

   ```
   cp .env.local.example .env.local
   ```

   **必須（これが無いとアプリが起動しません）**
   - `NEXT_PUBLIC_SUPABASE_URL`
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`

   **任意（未設定でもアプリは動きますが、該当機能が無効になります）**

   | 変数                        | 用途                             | 未設定時の挙動                     |
   | --------------------------- | -------------------------------- | ---------------------------------- |
   | `NEXT_PUBLIC_LIFF_ID`       | LINE アプリ内での起動            | 「LINEでログイン」が表示されない   |
   | `LINE_LOGIN_CHANNEL_ID`     | LINE ログインの検証              | LINE ログインが失敗する            |
   | `SUPABASE_SERVICE_ROLE_KEY` | LINE ログインのユーザー紐付け    | 同上                               |
   | `GEMINI_API_KEY`            | AI 機能                          | AI のボタンを押すとエラーになる    |
   | `GEMINI_MODEL`              | 使う AI モデルの変更（省略可）   | `gemini-3.5-flash` が使われる      |

   > ⚠️ **`.env.local` は絶対に Git にコミットしない**（`.gitignore` に入っているはず）。
   > 接続情報は Slack の DM など安全な経路で共有し、README やコミットには貼らないこと。
   >
   > ⚠️ **`SUPABASE_SERVICE_ROLE_KEY`・`LINE_LOGIN_CHANNEL_ID`・`GEMINI_API_KEY` に `NEXT_PUBLIC_` を付けないこと。**
   > 付けるとブラウザに露出します。特にサービスロール鍵は RLS をすべて迂回できるため、
   > 漏れると全データを読み書きされます。

   本番（Vercel）の環境変数は Project Settings → Environment Variables で設定します。
   **環境変数を追加・変更したら再デプロイが必要です**（既存のデプロイには反映されません）。

3. 開発サーバーを起動する。
   ```
   npm run dev
   ```
   ブラウザで `http://localhost:3000` を開く。

### Supabase について（重要）

このプロジェクトは **全員で1つの Supabase プロジェクトを共有** します（少人数なので、スキーマとデータの「正」を1箇所に集約するのが一番シンプルなため）。

- 現在 `supabase/migrations/` に15ファイルあり、すべて適用済みです。通常は自分で SQL を流す必要はありません。
- スキーマを変える migration を新しく追加した場合だけ、担当者が **Supabase ダッシュボード → SQL Editor** で流し、その旨をチームに共有します（二重実行を避けるため、流す人は1人に決める）。
- 共有DBなので、**テスト時に他人のデータを消さない**よう注意。動作確認用のグループ名には自分の名前を入れる等のルールにすると安全です。

---

## 4. Claude Code を使った開発の進め方

このプロジェクトは Claude Code で実装を進めます。全員が同じ流儀で使えるよう、**セッションの最初に下のプロンプトを貼ってから作業を始めてください。**

### セッション開始時に貼る共有プロンプト

```
このリポジトリはアウトドアサークル運営アプリ（Next.js App Router + TypeScript / Supabase / Vercel）です。
作業を始める前に、必ず docs/DESIGN.md を読んでください。

【前提】
- データモデル・ロール・命名規則・RLS 方針は docs/DESIGN.md が「正」。ここに書かれた定義に厳密に従うこと。
- Phase 0〜6 はすべて実装済み。さらに LINE 対応・計画書の様式編集・AI 補助を追加済み。

【守ってほしいルール】
1. 1回のセッションで扱うのは「1フェーズ」または「1つの明確なタスク」だけ。指定外のフェーズや機能には手を出さない。
2. DB スキーマを勝手に変えない。変更が必要と判断したら、まず理由を説明して提案し、承認を待つ。
   migration は既存ファイルを書き換えず、supabase/migrations に新しいファイルを追加する。
3. パスワード等の秘密情報を平文で保存・ログ出力しない。認証・権限は必ず RLS / RPC 側で担保する。
4. フォームは zod でバリデーション。更新系は Server Actions または RPC 呼び出しで実装する。
5. 変更後は npm run build が通ることを確認する。
6. 作業完了時に「変更したファイル一覧」「私（人間）が手動で確認すべきこと（Supabase の設定や SQL 実行を含む）」を箇条書きで報告する。

【今回のタスク】
〔ここに作業内容を書く。例: Phase 3（計画 + 行程表 B）を実装してください〕
```

### なぜこのルールなのか（読んでおくと事故が減る）

- **1フェーズずつ**: 一度に大量に生成させるとバグの温床になり、レビューもデバッグも困難になります。小さく作って動作確認、を繰り返します。
- **スキーマを勝手に変えない**: 全員で共有DBを使うので、勝手なスキーマ変更は他の人の環境を壊します。変更は必ず提案 → 合意 → migration 追加 → 共有、の順で。
- **手動確認の報告**: Supabase のバケット作成や SQL 実行など、コードだけでは完結しない作業があります。Claude Code に「人間がやるべきこと」を毎回リストさせると、抜けが減ります。

---

## 5. コーディング・運用ルール

- **ブランチ運用**: `main` に直接 push しない。フェーズや機能ごとにブランチを切る（例: `feature/phase3-itinerary`）。作業が終わったら Pull Request を出し、他の人が1人以上レビューしてから main にマージ。
- **1PR = 1まとまり**: フェーズ全部を1PRにせず、レビューしやすい単位に分ける。
- **DESIGN.md が正**: 仕様の疑問は DESIGN.md を確認。書いていない・迷う場合はチームで相談してから DESIGN.md を更新し、それから実装する（実装が先行して仕様がズレるのを防ぐ）。
- **migration は追記のみ**: 既存の migration ファイルは編集しない。変更は新しいファイルで。ファイル名は日時プレフィックス（例: `20240301000000_phase3_plans.sql`）。
- **秘密情報**: `.env.local`・Supabase のキー・グループのパスワードなどをコミットや公開チャンネルに貼らない。

---

## 6. これからの進め方

当初の Phase 0〜6 は完了しているため、以降は**フェーズ単位ではなく、改善テーマ単位**で担当を割り振ります。

残っているテーマは「2. 現在の進捗」の**「まだ手を付けていないこと」**を参照してください。

ただし、**次にやるべきは機能追加ではなく、実際にサークルのメンバーに使ってもらうこと**です。
ここまでの改善は設計上の推測に基づくもので、実際に利用者がつまずく場所は確認できていません。
配って使ってもらい、詰まった点を拾ってから次を決める方が確実です。

仕様の詳細は `docs/DESIGN.md` が「正」です。担当者は該当箇所を読み、
上の「共有プロンプト」の【今回のタスク】に落とし込んで Claude Code に依頼してください。

---

## 7. 困ったとき・確認コマンド

- 型チェック: `npx tsc --noEmit`
- Lint: `npx eslint .`
- ビルドが通るか: `npm run build`

> **コミット前に `npx tsc --noEmit` と `npx eslint .` を通してください。**
> `npm run build` の出力は長く、型エラーを見落としやすいためです。
- DB の中身を直接確認したいとき（Supabase → SQL Editor）:
  ```sql
  select * from group_members;   -- 誰がどのグループに何の役職で入っているか
  select * from plans;           -- 作成された計画
  ```
- 表示とDBの実態がズレていたら、まず SQL Editor で該当テーブルを直接見て切り分ける。
- RLS で「読めない・書けない」系のエラーは、DESIGN.md の RLS 方針とポリシーを確認する。

---

## 8. まだ決めていないこと（相談したい論点）

以下は今後チームで決める項目です（DESIGN.md の「今後の検討事項」にも記載）。

- グループの管理機能。**役職の変更と参加パスワードの変更は実装済み**（役職は本人が選ぶ方式、パスワードはグループ作成者のみ変更可）。メンバー削除・部長交代は未実装。
- 募集の定員超過時の競合制御（先着順を厳密にするか）。**未対応**。現状は同時アクセスで定員を超える可能性があります。
- ~~計画書の様式が行事種別で変わる場合のテンプレート切り替え~~ → **実装済み**。グループごとに表の項目を追加・削除・並べ替えできます（`/groups/[groupId]/document-template`）。
- アカウント統合。同じ人がメールと LINE で別アカウントになります。現状は「新しく入る人は LINE ログインを使う」運用で回避する方針です。
