-- ============================================================
-- 計画に「一人あたり予算（円）」を追加
--   ※ アーカイブの plan_reviews.cost_per_person（実際にかかった費用）とは別で、
--      これは計画時点の見込み予算。
-- ============================================================

alter table plans
  add column if not exists budget_per_person int;
